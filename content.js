// content.js

function extractEmailContent() {
    // Code to extract email content from Gmail
}

function sendToBackendService(content) {
    // Code to send content to the backend service
}

// Example usage
const emailContent = extractEmailContent();
sendToBackendService(emailContent);